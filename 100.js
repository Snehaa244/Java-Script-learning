//select element using query selector
const mainHeading=document.querySelector("main-heading")
const header = document.querySelector(".header");
const navItem=document.querySelectorAll("nav-item");//all is used for selecting all item of class varna sirdquerySelector se class ka first item hi select hoga..
console.log(navItem);